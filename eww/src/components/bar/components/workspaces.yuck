(deflisten workspace "scripts/workspaces")



(defwidget workspaces []
  (box :orientation "h"
     :halign "center"
     :valign "center"
     :class "workspaces"
     (literal :content workspace)))
