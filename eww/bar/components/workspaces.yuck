(defpoll workspace :interval "2s" "scripts/workspaceWayland")

(defvar current scripts/currentWorkspace)



(defwidget workspaces []
  (box :orientation "h"
     :halign "center"
     :valign "center"
     :class "workspaces"
     (literal :content workspace)))
